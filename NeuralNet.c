#include "NeuralNet.h"
#include <assert.h>

bool createNeuralNet(NeuralNet* network, const uint16* layerSizes, const uint8 totalLayerCount) {
    assert(network != NULL);
    assert(layerSizes != NULL);

    return false;
}

void deleteNeuralNet(NeuralNet* network) {
    assert(network != NULL);

    return false;
}